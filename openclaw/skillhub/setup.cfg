[metadata]
name = skillhub-cli
version = 0.1.0

[options]
package_dir =
    skillhub = .
packages =
    skillhub
    skillhub.adapters
    skillhub.commands
    skillhub.exceptions
    skillhub.interfaces
    skillhub.models
    skillhub.services
    skillhub.utils
python_requires = >=3.9
install_requires =
    typer
    rich
    pydantic
    pydantic-settings
    platformdirs
    httpx[http2]
    diskcache
    keyring
    keyrings.alt
    pyyaml

[options.extras_require]
test =
    pytest>=7.0.0
    pytest-asyncio>=0.21.0
    pytest-cov>=4.0.0
dev =
    pytest>=7.0.0
    pytest-asyncio>=0.21.0
    pytest-cov>=4.0.0
    ruff>=0.1.0
    mypy>=1.0.0

[options.entry_points]
console_scripts =
    skillhub = skillhub.cli:run
